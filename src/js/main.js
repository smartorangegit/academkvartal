(function() {
    // var svg = document.getElementById('main-page-svg')
    // var sky = document.getElementById('main-page-sky');
    // var top = sky.getBoundingClientRect().top;

    // function setTopOffset() {
    //     svg.style.top = sky.getBoundingClientRect().top + 'px';
    // };

    // var debouncedSetTopOffset = debounce(setTopOffset, 500)

    // window.addEventListener('resize', debouncedSetTopOffset)

})();